import React from 'react'
import { connect } from 'react-redux'
import { bindActionCreators } from 'redux'

const SearchRequestView = props => {
  return (
    <div>
      <h2>Search Request View</h2>
    </div>
  )
}

export default SearchRequestView
